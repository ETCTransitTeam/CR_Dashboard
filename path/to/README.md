# Snowflake JWT private key (required)
#
# On the server the app expects this exact path from the repo root:
#   path/to/key.p8
#
# Copy from a secure backup (do not commit the real key):
#   mkdir -p path/to
#   # paste or scp key.p8 here
#   chmod 600 path/to/key.p8
